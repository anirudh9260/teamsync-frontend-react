const alllowedConversions = {
    csv: ['.json', '.yaml'],
    json: ['.csv', '.yaml'],
    yaml: ['.csv', '.json'],
}

const extensions = ['xls', 'xlsx', 'json', 'xml', 'csv', 'yaml', 'txt']

export { alllowedConversions, extensions}